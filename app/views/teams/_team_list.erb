<b>
  <% if can? :edit, @project %>
    <% if defined? project %>
        <%= link_to 'New team', new_project_team_path(project), :remote => false, :class => 'dashed_link transition new-team' %>
    <% end %>
<% end %>
</b>
<%= render :partial => 'teams/team_listing', :locals => {:project => @project, :teams => @teams.present? ? @teams : @project.teams}%>

